# .aprx files are committed but treated as binary so git never attempts a text
# merge on the zip. Binary conflicts are resolved automatically: the pre-commit
# hook always regenerates the .aprx from the canonical .aprx.src directory.
*.aprx binary
